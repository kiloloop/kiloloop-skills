__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/
build/
.venv/
venv/
.env
.env.*
*.pem
*.key
.DS_Store

# Coding-agent runtime artifacts: per-checkout working files describing how an
# agent operates locally, never part of the distributed collection. Kept
# untracked so `git add -A` in a fresh clone cannot pick them up.
.claude/
.codex/
.worktrees/
AGENTS.md
CLAUDE.md
